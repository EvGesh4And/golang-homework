apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: calendar-grpc-ingress
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
spec:
  rules:
    - host: calendar-grpc.local
      http:
        paths:
          - path: /grpc
            pathType: Prefix
            backend:
              service:
                name: calendar-service
                port:
                  name: grpc
